def find_max_min(arr):
    maximum = minimum = arr[0]
    for num in arr[1:]:
        if num > maximum:
            maximum = num
        if num < minimum:
            minimum = num
    return maximum, minimum
    

array = [12, 45, 2, 67, 23, 89, 5]
print(find_max_min(array))
